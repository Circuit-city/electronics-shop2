import Checkout from '../Checkout';

describe('Checkout', () => {
    test('renders the Checkout component', () => {
      ( Checkout );
    });
  });

test ('test', ()=>{
    expect(true).toBe(true);
})